# Codex Security High Findings Review

Reviewed scan `1883ac77a02881919adabeb5408013ec` and finding link `4e75de9fbc948191871a6e737f52fb76` for `Samfresh-ai/Sentinel` at the June 3, 2026 scan state.

| Finding | Severity | File/Lines | Risk | Suggested Fix | Decision | Reason | Status |
|--------|----------|------------|------|---------------|----------|--------|--------|
| `4e75de9fbc948191871a6e737f52fb76` - Authenticated users can poison Splunk prod logs | High | `apps/api/src/app.ts` lines 65-80, 408-415, 522-546, 1212-1229 | Real. Normal JWT users could submit project logs into trusted `index=prod sourcetype=app`, polluting service health, saved searches, and Sentinel investigations. | Isolate browser project logs to a dedicated sourcetype/index, add `orgId`, scope saved search/count queries, and rate-limit `/projects/:id/logs`. | Modified Codex suggestion | Kept the safe shape but implemented it manually against current code: `sentinel:project-app-log`, configured Sentinel index, org/project SPL filter, and per-org/project KV rate limit. | Fixed |
| `d797017e560c8191bf131b81f975d7fb` - AWS flow disables Splunk TLS validation in production | High | `deploy/aws/splunk-default.yml` lines 2-6, `docker-compose.aws.yml` lines 17-47, `.env.aws.example`, `packages/splunk-brain/src/client.ts` lines 82-90 | Real. Production AWS config allowed plaintext HEC and disabled TLS verification for non-local Splunk, exposing Splunk admin/HEC secrets to MITM or malicious endpoints. | Enable HTTPS HEC, keep TLS verification enabled, remove unsafe defaults, add production readiness gates. | Modified Codex suggestion | Implemented secure transport enforcement in client and runtime readiness, updated AWS examples/compose to HTTPS gateway/TLS validation, and preserved gateway token forwarding. | Fixed |
| `034a6ef4016c8191bc0f2ca9813e408c` - Arbitrary SPL exposed through agent tool API | High | `apps/api/src/app.ts` lines 93-110 and 693-699, `packages/agent/src/tools/query-splunk-logs.ts` lines 6-107, `packages/agent/src/tool-json-schemas.ts`, `packages/agent/src/agent-config.ts` | Real. Any holder of the agent tool secret could send raw SPL through `/agent/tools/query_splunk_logs`, expanding tool-secret compromise or prompt-injection blast radius. | Remove caller-controlled `spl`, require structured service/time inputs, generate bounded SPL internally, update tool schema and instructions. | Accepted shape, manual implementation | Public schema now requires `services`; Zod schema is strict; runner verify flow uses service/symptom inputs instead of raw SPL. | Fixed |
| `7ae58deb7a648191b208966de3ee9724` - Custom Splunk CA disables TLS hostname verification | High | `deploy/render/README.md` lines 89-92, `packages/splunk-brain/src/client.ts` lines 72 and request options | Real. `SPLUNK_CA_CERT` still disabled hostname/SAN checks while sending Basic auth and HEC token. | Remove `checkServerIdentity` bypass while keeping custom CA support. | Accepted shape, manual implementation | Removed hostname-mismatch bypass from `requestRaw` callers/type and updated docs to require matching endpoint hostname. | Fixed |
| `b41fc1efa6d081918c0bc21ea46b666e` - Malformed absolute URL crashes combined proxy | High | `apps/api/docker/sentinel-combined-server.mjs` lines 44-87, `apps/api/Dockerfile` line 44 | Real. Unauthenticated malformed absolute-form request targets could throw in `new URL()` and terminate the combined API/web process. | Safely parse request target and return 400 on invalid URL before proxying. | Accepted shape, manual implementation | Added `parseRequestPathname` with a 400 JSON response path and preserved existing web/API routing. | Fixed |
| `f696887482548191a78d4b79375bc9e9` - Splunk tunnel gateway fails open without token | High | `deploy/render/README.md` line 58, `scripts/splunk-tunnel-gateway.ts` lines 7-43 | Real. Gateway auth returned true when `SPLUNK_GATEWAY_TOKEN` was unset and buffered request bodies without a size limit. | Require token, constant-time compare, add body-size cap, document token as required. | Accepted shape, manual implementation | Gateway now fails closed, returns 401 without token, enforces `SPLUNK_GATEWAY_MAX_BODY_BYTES`, and returns 413 on oversized bodies. | Fixed |
| `29774599a40c81918de624997f51258f` - Plaintext Splunk tunnel URLs can leak credentials | High | `packages/shared/src/runtime-mode.ts` lines 115-123, `packages/splunk-brain/src/client.ts` lines 37-44 and request paths | Real. Non-local `http://` Splunk management/HEC endpoints could pass production readiness and receive Basic auth, HEC, and gateway/CF headers. | Reject non-local HTTP in client and production readiness; allow only local dev HTTP. | Accepted shape, manual implementation | Client now throws before sending secrets to non-local HTTP endpoints; readiness flags non-local HTTP and TLS-disabled production endpoints. | Fixed |
| `25c8f61cd15081919d0019dbbfea53af` - Admin remediation leaks agent tool secret | High | `apps/api/src/app.ts` lines 93-110 and 1334-1358, `packages/agent/src/tools/execute-remediation.ts` lines 65-290, `packages/shared/src/runtime-mode.ts` lines 85-105 | Real. Admin remediation forwarded the central `AGENT_TOOL_SECRET`/`WEBHOOK_SECRET` to service admin endpoints, allowing downstream reuse against `/agent/tools`. | Use separate least-privileged remediation secret, require HTTPS/allowlisted admin origins, keep `AGENT_TOOL_SECRET` internal. | Modified Codex suggestion | Added `SENTINEL_ADMIN_REMEDIATION_SECRET`, `x-sentinel-remediation-secret`, production readiness/schema support, and admin URL validation. | Fixed |
| `048414989094819196f200ca557e34e4` - DLQ flush lets any tenant retry other org incidents | High | `apps/api/src/app.ts` lines 904-949 and 1590-1594, `packages/splunk-brain/src/kvstore.ts` line 143 | Real. Authenticated tenants could call forced DLQ flush, scan all in-progress incidents, and mutate/retry other org incidents. | Pass caller `orgId` to forced flush, use org-scoped queries, and key DLQ by org plus incident. | Accepted shape, manual implementation | Forced route now scopes to `auth.orgId`; `dead_letter` is org-scoped; new DLQ keys are SHA-256 of `orgId:incidentId` with safe legacy lookup. | Fixed |
| `6febd89c08288191baf8c00cdb83407c` - Qdrant memory search leaks data across tenants | High, closed | Archived finding page; old mixed Qdrant/OperaIQ paths: `packages/splunk-brain/src/search.ts`, `packages/splunk-mcp/src/index.ts`, `packages/agent/src/tools/query-splunk-logs.ts` | No longer present in current Sentinel tree. Codex Security automatically closed it because the issue is no longer detected after the Sentinel/OperaIQ separation. | Org-scope Qdrant fallback search or remove Qdrant path from Sentinel. | Rejected as current false positive / closed finding | Current Sentinel scan of the repo contains no Qdrant/OperaIQ runtime code outside the negative assertion in `scripts/security-fix-checks.ts`; no Sentinel patch required. | Accounted for; no code fix |

